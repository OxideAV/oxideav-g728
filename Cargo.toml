[package]
name = "oxideav-g728"
version = "0.0.6"
edition = "2021"
rust-version = "1.80"
license = "MIT"
authors = ["Mark Karpeles"]
description = "Pure-Rust ITU-T G.728 Low-Delay CELP (LD-CELP, 16 kbit/s) codec — decoder + encoder"
repository = "https://github.com/OxideAV/oxideav-g728"
homepage = "https://github.com/OxideAV/oxideav-g728"
readme = "README.md"
keywords = ["multimedia", "audio", "g728", "telephony", "codec"]
categories = ["multimedia::encoding", "multimedia::audio"]

[dependencies]
oxideav-core = "0.1"
# Distributed-slice auto-registration into `oxideav_core::REGISTRARS`
# via the `oxideav_core::register!` macro. Tiny crate, no transitive
# deps; works on every target including wasm.
linkme = "0.3"
